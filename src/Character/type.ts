/// Represents the different types of characters in the game.
///
/// # Description
///
/// An enum where each character type corresponds to a number.
///
/// # Values
///
/// * `TRIANGLE` - Represents a triangle character type (0)

export enum CharacterType {
  TRIANGLE = 0,
}
